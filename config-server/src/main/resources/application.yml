server:
  port: 8888
spring:
  application:
    name: :config-server
  cloud:
    config:
      server:
        git:
          uri: http://localhost:3000/test/config
#          uri: https://github.com/habuma/tacocloud-config
encrypt:
  key: s3cr2t